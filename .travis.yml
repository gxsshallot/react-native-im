language: node_js
node_js:
 - "10"
os:
 - linux
cache: yarn

script: echo "yarn test temporarily disabled"

stages:
 - test
 - name: deploy
   if: tag = true

jobs:
 include:
   - stage: test
     name: eslint
     script: yarn run eslint
   - stage: deploy
     deploy:
       provider: npm
       email: gaoxiaosong06@gmail.com
       api_key: "$NPM_TOKEN"
       on:
         tags: true
